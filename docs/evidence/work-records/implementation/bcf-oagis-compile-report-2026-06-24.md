---
title: BCF × OAGIS A0 Compile / Preflight Report (2026-06-24)
description: Deterministic A0 compile output. Read-only against substrate; no LLM, no panels, no writes. Produces exact executable ledger seed + per-bucket/Pass/wave counts + operator decision queue. Per blueprint §6A.
status: held
authority: a0-compile-preflight
date: 2026-06-24
project: bc-docs-v3
domain: contracts
subdomain: catalog
focus: bcf-oagis-a0-compile
coordinator_run_id: CRR-a0c24a
substrate_snapshot_hash: sha256:8bcfa7a0bd220e304d2526574f2e0a18c8aa5bcc9a3e5bc8557d76273f46653d
related_docs:
  - bcf-oagis-broad-buildout-blueprint-2026-06-23.md
  - bcf-oagis-retry-ledger-2026-06-24.md
---

# BCF × OAGIS A0 Compile / Preflight Report (2026-06-24)

> **Authority.** Generated by deterministic A0 compile per blueprint §6A. Read-only against substrate. No LLM. No panels. No writes. No certifications. This report + the sibling retry ledger seed (`bcf-oagis-retry-ledger-2026-06-24.md`) are the substrate-of-record for the BCF × OAGIS program, pending operator A1–A5 program approval.
>
> **A0 / A1–A5 sequencing.** A0 may run before A1–A5 (§6A.0). A1–A5 is required before any panel call, entity admission, characteristic admission, BC authoring, certification, or substrate write. This A0 run has performed **none** of those.

## 0. Run identity

| Field | Value |
|---|---|
| Coordinator run id | `CRR-a0c24a` |
| Substrate snapshot hash | `sha256:8bcfa7a0bd220e304d2526574f2e0a18c8aa5bcc9a3e5bc8557d76273f46653d` |
| OAGIS extract | `data/oagis-finance-extract-enriched-2026-05-15.json` |
| OAGIS noun count | 133 |
| Total scalar fields classified | 2868 |
| Substrate entities (active) | 26 |
| Substrate characteristics (active) | 62 |
| Substrate value BCs (active) | 194 |

## 1. Exact bucket counts

> **Mislabel correction (see §19).** This blueprint table reports **two views** because the bucket assignment is mutually exclusive at the ledger row / candidate row level, but multi-label at the scalar-field level for 46 role-split-mapped fields. Sums must be checked against the explicit base of each view.

### 1.A View A — Per-scalar-field primary bucket (mutually exclusive; sums to 2868)

Each OAGIS scalar field is assigned to **exactly one** primary bucket. For role-split-mapped fields (46 fields across 6 nouns; see §19) the alphabetically-first target entity determines the primary bucket; the second target generates a secondary candidate row tracked in View B.

| Bucket | Per-scalar-field primary | % of 2,868 |
|---|---|---|
| 1 EXISTING_ENTITY_EXISTING_CHARACTERISTIC_BC | 112 | 3.9% |
| 2 EXISTING_ENTITY_NEW_CHARACTERISTIC | 297 | 10.4% |
| 3 NEW_ENTITY_EXISTING_CHARACTERISTIC | 108 | 3.8% |
| 4 NEW_ENTITY_NEW_CHARACTERISTIC | 363 | 12.7% |
| 5 RUNTIME_DERIVED_DROP | 48 | 1.7% |
| 6 METADATA_DROP | 1,561 | 54.4% |
| 7 SOURCE_DIAGNOSTIC_DEFER | 91 | 3.2% |
| 8 SEMANTIC_AMBIGUITY_HALT | 288 | 10.0% |
| **Total scalar fields** | **2,868** | **100.0%** ✓ |

### 1.B View B — Per-candidate-row count (sums to 2914 = 2868 + 46 role-split secondary)

Each role-split-mapped scalar field generates **two candidate rows** (one per target entity); both rows share the same bucket because role-split entity pairs are class-consistent (e.g., Invoice → both Customer Invoice + Supplier Invoice are existing; project-accounting → both Project + Cost Centre are new). View B is the natural unit of the ledger because each candidate row gets its own `candidate_id`, `packet_hash`, and downstream writer queue position.

| Bucket | Per-candidate-row count | % of 2,914 |
|---|---|---|
| 1 EXISTING_ENTITY_EXISTING_CHARACTERISTIC_BC | 127 | 4.4% |
| 2 EXISTING_ENTITY_NEW_CHARACTERISTIC | 321 | 11.0% |
| 3 NEW_ENTITY_EXISTING_CHARACTERISTIC | 108 | 3.7% |
| 4 NEW_ENTITY_NEW_CHARACTERISTIC | 370 | 12.7% |
| 5 RUNTIME_DERIVED_DROP | 48 | 1.6% |
| 6 METADATA_DROP | 1,561 | 53.6% |
| 7 SOURCE_DIAGNOSTIC_DEFER | 91 | 3.1% |
| 8 SEMANTIC_AMBIGUITY_HALT | 288 | 9.9% |
| **Total candidate rows** | **2,914** | **100.0%** ✓ |

Difference View B − View A = **46** role-split secondary rows. See §19 for the per-noun breakdown and schema rationale.

## 2. Deduplicated BC target counts (executable backlog)

After deduplication on (entity, characteristic) tuples:

| Bucket | BC targets |
|---|---|
| 1 EXISTING_ENTITY_EXISTING_CHARACTERISTIC_BC | **78** |
| 2 EXISTING_ENTITY_NEW_CHARACTERISTIC | **238** |
| 3 NEW_ENTITY_EXISTING_CHARACTERISTIC | **83** |
| 4 NEW_ENTITY_NEW_CHARACTERISTIC | **334** |
| **TOTAL BC TARGETS** | **733** |

## 3. Execution-class split

| Class | Rows | Notes |
|---|---|---|
| GREEN | 27 | Whitelist EE_EC_BC reuse — autonomous-safe under §8.2 |
| AMBER | 639 | Need pin_evidence_text per row before autonomous execution |
| RED | 67 | Held; composite-identity entities + first-of-kind |
| DEFER | 1700 | Metadata + runtime-derived + source-diagnostic |
| UNKNOWN | 288 | SEMANTIC_AMBIGUITY_HALT + S2 violations; operator triage |

## 4. Per-Pass counts (executable backlog only — GREEN/AMBER/RED)

| Pass | Rows | Notes |
|---|---|---|
| Pass 1 (C-waves) — new characteristic admissions | 217 unique bf_names | Pre-resolves vocabulary for all downstream BCs that reference these |
| Pass 2 (E-waves) — new entity admissions | 54 unique entities | ~47 simple-AMBER + ~7 composite-RED |
| Pass 3 (BC-waves) — BC binding | 733 | All BC target rows |

## 5. Per-wave executable counts

| Wave | GREEN | AMBER | RED | DEFER | UNKNOWN | Total |
|---|---|---|---|---|---|---|
| BC-AP | 11 | 122 | 0 | 0 | 0 | 133 |
| BC-AR | 13 | 140 | 0 | 0 | 0 | 153 |
| BC-Asset | 0 | 50 | 6 | 0 | 0 | 56 |
| BC-Bank | 0 | 4 | 0 | 0 | 0 | 4 |
| BC-GL | 3 | 23 | 0 | 0 | 0 | 26 |
| BC-Inventory | 0 | 11 | 12 | 0 | 0 | 23 |
| BC-Logistics | 0 | 83 | 0 | 0 | 0 | 83 |
| BC-Master | 0 | 86 | 0 | 0 | 0 | 86 |
| BC-Procurement | 0 | 35 | 0 | 0 | 0 | 35 |
| BC-Production-composite | 0 | 0 | 27 | 0 | 0 | 27 |
| BC-Production-simple | 0 | 38 | 0 | 0 | 0 | 38 |
| BC-Quality-composite | 0 | 0 | 22 | 0 | 0 | 22 |
| BC-Quality-simple | 0 | 27 | 0 | 0 | 0 | 27 |
| BC-Reference | 0 | 16 | 0 | 0 | 0 | 16 |
| BC-Workforce | 0 | 4 | 0 | 0 | 0 | 4 |

## 6. Proposed new entities (54)

- Asset — slice=asset-maintenance-simple
- Bill of Materials — slice=production-simple
- Bill of Materials Line (composite-RED) — slice=production-composite
- Budget Ledger Entry — slice=reference-warranty-budget
- Budget Ledger Entry Line — slice=reference-warranty-budget
- Business Partner — slice=master-data
- Certificate — slice=quality-simple
- Corrective Action — slice=quality-simple
- Cost Centre — slice=master-data
- Delivery — slice=logistics
- Employee — slice=workforce
- Equipment — slice=asset-maintenance-simple
- Inspection Lot (composite-RED) — slice=quality-composite
- Inventory Count — slice=inventory-composite
- Inventory Count Line — slice=inventory-composite
- Inventory Movement — slice=inventory-composite
- Inventory Movement Line — slice=inventory-composite
- Inventory Position (composite-RED) — slice=inventory-composite
- Item — slice=master-data-root
- Location — slice=master-data
- Maintenance Order — slice=asset-maintenance-simple
- Maintenance Order Operation (composite-RED) — slice=maintenance-composite
- Manufacturing Process — slice=production-simple
- Nonconformance Notification — slice=quality-simple
- Nonconformance Notification Line — slice=quality-simple
- Operation (composite-RED) — slice=production-composite
- Org Unit — slice=master-data
- Outline Agreement — slice=procurement-transactional
- Outline Agreement Line — slice=procurement-transactional
- Party — slice=master-data
- Pick List — slice=logistics
- Pick List Line — slice=logistics
- Price List — slice=master-data
- Price List Item — slice=master-data
- Production Confirmation — slice=production-simple
- Production Schedule — slice=production-simple
- Project — slice=master-data
- Purchase Requisition — slice=procurement-transactional
- Purchase Requisition Line — slice=procurement-transactional
- Quote — slice=procurement-transactional
- Quote Line — slice=procurement-transactional
- RFQ — slice=procurement-transactional
- RFQ Line — slice=procurement-transactional
- Routing — slice=production-simple
- Shipment Request — slice=logistics
- Shipment Unit — slice=logistics
- Test Method — slice=quality-simple
- Test Result (composite-RED) — slice=quality-composite
- Three-Way Match Document — slice=logistics
- Three-Way Match Document Line — slice=logistics
- Warranty Claim — slice=reference-warranty-budget
- Work Centre — slice=production-simple
- Work Order — slice=production-simple
- Work Order Operation (composite-RED) — slice=production-composite

## 7. Proposed new characteristics — top 50 by BC-binding count

| # | bf_name | Inferred rep_term | Binding count (S1 used_by_bc_target_count) | Target entities |
|---|---|---|---|---|
| 1 | `__NEW__type_code` | code | 61 | Customer Invoice Line Item, Supplier Invoice Line, Three-Way Match Document, Three-Way Match Document Line, Journal Entry Line, ... |
| 2 | `__NEW__gl_entity_identifier` | identifier | 13 | Supplier Invoice, Three-Way Match Document, Journal Entry, Customer Invoice, Budget Ledger Entry, ... |
| 3 | `__NEW__amount` | amount | 13 | Supplier Invoice, Supplier Invoice Line, Three-Way Match Document, Three-Way Match Document Line, Journal Entry, ... |
| 4 | `__NEW__system_identifier` | identifier | 10 | Purchase Order, Purchase Requisition, RFQ, Quote, Sales Order, ... |
| 5 | `__NEW__tax_base_amount` | amount | 9 | Supplier Invoice, Supplier Invoice Line, Three-Way Match Document, Three-Way Match Document Line, Journal Entry, ... |
| 6 | `__NEW__functional_amount` | amount | 9 | Supplier Invoice, Supplier Invoice Line, Three-Way Match Document, Three-Way Match Document Line, Journal Entry, ... |
| 7 | `__NEW__tax_base_functional_amount` | amount | 9 | Supplier Invoice, Supplier Invoice Line, Three-Way Match Document, Three-Way Match Document Line, Journal Entry, ... |
| 8 | `__NEW__required_delivery_date_time` | date | 9 | Purchase Order Line, Purchase Requisition, Purchase Requisition Line, RFQ Line, Quote Line, ... |
| 9 | `__NEW__payment_method_code` | code | 8 | Three-Way Match Document, Remittance Advice, Purchase Order, Sales Order, Party, ... |
| 10 | `__NEW__serial_number_identifier` | identifier | 8 | Inventory Position, Customer Shipment, Shipment Unit, Goods Receipt Line, Bill of Materials Line, ... |
| 11 | `__NEW__dunnage_weight_measure` | quantity | 7 | Customer Invoice, Supplier Invoice, Supplier Invoice Line, Customer Shipment, Shipment Unit, ... |
| 12 | `__NEW__tare_weight_measure` | quantity | 7 | Customer Invoice, Supplier Invoice, Supplier Invoice Line, Customer Shipment, Shipment Unit, ... |
| 13 | `__NEW__ledger_identifier` | identifier | 6 | Supplier Invoice, Three-Way Match Document, Journal Entry, Customer Invoice, Credit Status, ... |
| 14 | `__NEW__cost_center_identifier` | identifier | 6 | Purchase Order, Purchase Requisition, RFQ, Quote, Sales Order, ... |
| 15 | `__NEW__promised_ship_date_time` | date | 6 | Purchase Order, Purchase Order Line, Quote Line, Customer Shipment, Sales Order, ... |
| 16 | `__NEW__promised_delivery_date_time` | date | 6 | Purchase Order, Purchase Order Line, Quote Line, Customer Shipment, Sales Order, ... |
| 17 | `__NEW__shipping_instructions` | text | 6 | Purchase Order, Purchase Order Line, Shipment Request, Delivery, Sales Order, ... |
| 18 | `__NEW__transaction_date_time` | date | 6 | Inventory Position, Inventory Movement, Inventory Movement Line, Inventory Count Line, Goods Receipt Line, ... |
| 19 | `__NEW__transportation_method_code` | code | 5 | Customer Invoice, Supplier Invoice, Customer Shipment, Shipment Request, Goods Receipt |
| 20 | `__NEW__requested_delivery_date` | date | 5 | Customer Invoice, Supplier Invoice, Customer Invoice Line Item, Supplier Invoice Line, Delivery |
| 21 | `__NEW__gl_destination_entity_identifier` | identifier | 5 | Supplier Invoice Line, Three-Way Match Document Line, Journal Entry Line, Customer Invoice Line Item, Journal Entry |
| 22 | `__NEW__special_price_authorization_code` | code | 5 | Three-Way Match Document, Purchase Order, Purchase Order Line, Sales Order, Sales Order Line |
| 23 | `__NEW__account_identifier` | identifier | 5 | Credit Application, Party, Business Partner, Supplier, Customer |
| 24 | `__NEW__government_issued_party_identifier` | identifier | 5 | Credit Application, Party, Business Partner, Supplier, Customer |
| 25 | `__NEW__uid` | identifier | 5 | Inventory Position, Customer Shipment, Shipment Unit, Goods Receipt Line, Bill of Materials Line |
| 26 | `__NEW__reference_designator_identifier` | identifier | 5 | Inventory Position, Customer Shipment, Shipment Unit, Goods Receipt Line, Bill of Materials Line |
| 27 | `__NEW__find_number_identifier` | identifier | 5 | Inventory Position, Customer Shipment, Shipment Unit, Goods Receipt Line, Bill of Materials Line |
| 28 | `__NEW__expiration_date` | date | 5 | Inventory Position, Customer Shipment, Shipment Unit, Goods Receipt Line, Bill of Materials Line |
| 29 | `__NEW__best_used_by_date` | date | 5 | Inventory Position, Customer Shipment, Shipment Unit, Goods Receipt Line, Bill of Materials Line |
| 30 | `__NEW__incoterms_code` | code | 5 | Shipment Request, Party, Business Partner, Supplier, Customer |
| 31 | `__NEW__inco_terms_place_name` | text | 5 | Shipment Request, Party, Business Partner, Supplier, Customer |
| 32 | `__NEW__authorization_identifier` | identifier | 4 | Customer Payment, Vendor Payment, Delivery, Project |
| 33 | `__NEW__short_name` | text | 4 | RFQ, Shipment Unit, Work Centre, Org Unit |
| 34 | `__NEW__scheduled_delivery_date_time` | date | 4 | Customer Shipment, Goods Receipt, Sales Order Line, Inspection Lot |
| 35 | `__NEW__estimated_weight_measure` | quantity | 4 | Customer Shipment, Shipment Unit, Goods Receipt, Goods Receipt Line |
| 36 | `__NEW__loading_weight_measure` | quantity | 4 | Customer Shipment, Shipment Unit, Goods Receipt, Goods Receipt Line |
| 37 | `__NEW__distribution_center_code` | code | 4 | Customer Shipment, Goods Receipt, Sales Order, Sales Order Line |
| 38 | `__NEW__freight_item_identifier` | identifier | 4 | Customer Shipment, Shipment Unit, Item, Equipment |
| 39 | `__NEW__container_identifier` | identifier | 4 | Customer Shipment, Shipment Unit, Goods Receipt, Delivery |
| 40 | `__NEW__received_date_time` | date | 4 | Delivery, Goods Receipt, Inspection Lot, Test Result |
| 41 | `__NEW__required_date_time` | date | 4 | Delivery, Work Order, Work Order Operation, Production Schedule |
| 42 | `__NEW__base_uom_code` | code | 4 | Bill of Materials, Asset, Item, Equipment |
| 43 | `__NEW__payment_term_identifier` | identifier | 4 | Party, Business Partner, Supplier, Customer |
| 44 | `__NEW__creation_date_time` | date | 4 | Party, Business Partner, Supplier, Customer |
| 45 | `__NEW__total_value_amount` | amount | 3 | Customer Invoice, Supplier Invoice, Goods Receipt Line |
| 46 | `__NEW__earliest_ship_date_time` | date | 3 | Purchase Order, Customer Shipment, Sales Order |
| 47 | `__NEW__storage_uom_code` | code | 3 | Inventory Position, Item, Equipment |
| 48 | `__NEW__actual_ship_date_time` | date | 3 | Customer Shipment, Goods Receipt, Sales Order Line |
| 49 | `__NEW__freight_term_code` | code | 3 | Customer Shipment, Shipment Request, Goods Receipt |
| 50 | `__NEW__ownership_code` | code | 3 | Customer Shipment, Shipment Unit, Goods Receipt Line |

Total unique new-characteristic bf_names: **217**. (Operator review at A1–A5 will reclassify ~70–80 as SOURCE_DIAGNOSTIC_DEFER and coalesce others; estimated admitted count after review: ~120–180.)

## 8. Idempotent rows (already active in substrate)

A0 detected **31** (entity, characteristic) tuples whose proposed BC already exists active in the substrate. These rows are flagged `terminal_outcome_code=authored_idempotent` at compile time; the writer would skip them under §6C.2 pre-write recheck.

_Sample (first 20):_

- `Customer Invoice|document date`
- `Supplier Invoice|document date`
- `Customer Invoice Line Item|line number`
- `Supplier Invoice Line|line number`
- `Customer Invoice Line Item|line extension amount`
- `Supplier Invoice Line|line extension amount`
- `Journal Entry|document date`
- `Journal Entry Line|line number`
- `Remittance Advice|document date`
- `GL Account|account type code`
- `Customer Payment|document date`
- `Vendor Payment|document date`
- `Currency Exchange Rate|source currency code`
- `Currency Exchange Rate|target currency code`
- `Purchase Order|document date`
- `Purchase Order Line|line number`
- `Purchase Order Line|line extension amount`
- `Customer Shipment|document date`
- `Customer Shipment|delivery date`
- `Customer Shipment|net weight`

## 9. Estimated panel-call count + spend range

| Pass | Panel calls (est.) | Avg latency (s) | Per-row estimate (avg) | Subtotal (s) |
|---|---|---|---|---|
| Pass 1 (~217 new chars) | 217 | 60 | 217×60 | 13020 |
| Pass 2 (~54 new entities) | 54 | 60 | 54×60 | 3240 |
| Pass 3 (~702 BC bindings excluding idempotent) | 702 | 55 | 702×55 | 38610 |
| **Total panel calls** | **973** | | | ~15.2 hours panel wall-time |

Spend range (per-row estimated $0.05–$0.20 per panel — bc-ai Maker / Checker / Moderator at current pricing):

| Scenario | Per-row cost | Total panel rows | Estimated spend |
|---|---|---|---|
| Low (cache-warm, low-token panels) | $0.05 | 973 | $48.65 |
| Mid (mixed cache, normal panels) | $0.10 | 973 | $97.30 |
| High (cold cache, high-token panels) | $0.20 | 973 | $194.60 |

These are A0-time estimates. Actual `model_cost_actual` is recorded per row at panel-response time and tracked against A5 caps per §6C.4.

## 10. Blueprint range vs A0 exact

| Surface | Blueprint range (§4A) | A0 exact |
|---|---|---|
| OAGIS scalar fields classified | 2,868 | 2868 |
| METADATA_DROP | 1,561 (54.4%) | 1561 (54.4%) |
| RUNTIME_DERIVED_DROP | 48 (1.7%) | 48 (1.7%) |
| SOURCE_DIAGNOSTIC_DEFER | 44 (1.5%) | 91 (3.2%) |
| SEMANTIC_AMBIGUITY_HALT | 290 (10.1%) | 288 (10.0%) |
| Deduplicated BC targets | 773 | **733** |
| Proposed new entities | 54 | **54** |
| Proposed new char bf_names | 224 | **217** |

## 11. Safeguard enforcement

| Safeguard | Result |
|---|---|
| S1 — Characteristic must support ≥1 target BC | Pass. 217 proposed new chars; each carries `used_by_bc_target_count` ≥ 1 by construction. |
| S2 — Entity must belong to a coherent slice | Pass. All 54 proposed new entities have non-null entity_slice_name. |
| No RED execution row | Pass. 67 RED rows present; **all held** (terminal_outcome_code=red_held; never autonomously executed per §8.2). |
| No metadata/runtime/source-diagnostic admission | Pass. 1700 rows correctly routed to DEFER. |
| No OAGIS term admitted as identity authority | Pass. All admitted characteristics carry OAGIS as Tier-T2 evidence + BareCount-owned term; no OAGIS bf_name admitted verbatim as the canonical term without F4-v2 v1 admission (operator decision per row at A1–A5). |

## 12. Top 20 SEMANTIC_AMBIGUITY_HALT clusters (operator triage queue)

- carrier-route: 23 rows
- planning-schedule: 15 rows
- move-product: 14 rows
- move-product-forecast: 14 rows
- opportunity: 14 rows
- technical-data-package: 14 rows
- product-availability: 10 rows
- sales-lead: 10 rows
- require-product: 9 rows
- log-book: 9 rows
- shippers-letter-of-instruction: 8 rows
- online-session: 8 rows
- hazardous-material-shipment-document: 7 rows
- test-specification: 7 rows
- repair-status: 7 rows
- shippers-export-declaration: 6 rows
- catalog: 6 rows
- corrective-action-plan: 6 rows
- scale-ticket: 6 rows
- online-order: 5 rows

## 13. Top 20 high-value GREEN/AMBER groups

- BC-AR (AMBER): 140 rows
- BC-AP (AMBER): 122 rows
- BC-Master (AMBER): 86 rows
- BC-Logistics (AMBER): 83 rows
- BC-Asset (AMBER): 50 rows
- BC-Production-simple (AMBER): 38 rows
- BC-Procurement (AMBER): 35 rows
- BC-Quality-simple (AMBER): 27 rows
- BC-GL (AMBER): 23 rows
- BC-Reference (AMBER): 16 rows
- BC-AR (GREEN): 13 rows
- BC-AP (GREEN): 11 rows
- BC-Inventory (AMBER): 11 rows
- BC-Bank (AMBER): 4 rows
- BC-Workforce (AMBER): 4 rows
- BC-GL (GREEN): 3 rows

## 14. Recommended A1–A5 settings

Based on this A0 run, the suggested operator A1–A5 settings:

| Approval | Recommended setting |
|---|---|
| **A1 — Target-state catalogue + admission rule** | Approve §1 Evidence-Governed Foundation Buildout + §4A target catalogue + §4A.10 AR distribution + §4B layer-first re-aggregation. Catalogue counts: 26 active entities → +54 proposed; 62 active chars → +217 proposed unique bf_names; 194 active BCs → +733 proposed targets. |
| **A2 — Row classification + admission rules** | Approve §5.1 + §8.2 row rules + §1.2 admission rationale validation (each row carries ≥1 AR code with evidence) + §1.4 safeguards 1+2. |
| **A3 — Halt rules** | Approve §5.3 per-(C/E/BC)-wave halt rules + §8.4 fatal stops + §6A compile-layer halt pre-conditions. |
| **A4 — Retry ledger + Compile + Cache** | Approve §6 ledger + §6A Compile / Preflight Layer + §6B Batch + Cache + §6C Coordinator / Workers / Single Writer + §6.7 program completion state. This A0 run instantiates §6A; the retry ledger seed file is the sibling `bcf-oagis-retry-ledger-2026-06-24.md`. |
| **A5 — Budget / time caps** | Recommended starting caps (operator adjusts): (a) total program spend cap **$200–$400**; (b) total panel-call cap **1216** (executable + 25% retry buffer); (c) optional pass-level caps: Pass 1 **$50 / 237 calls**, Pass 2 **$30 / 64 calls**, Pass 3 **$200 / 752 calls**; (d) per-row latency cap **180 seconds**; (e) wall-time cap **24 hours**. |

## 15. Is A1–A5 safely considerable next?

**Yes — with two caveats.**

| Check | Result |
|---|---|
| A0 ran read-only / no-LLM / no-write | ✓ |
| Substrate snapshot loaded (26 active entities, 62 active chars, 194 active BCs) | ✓ |
| All 2,868 scalar fields classified into 8 buckets | ✓ |
| Every executable row has a packet_hash + substrate_snapshot_hash + evidence_hash | ✓ |
| S1 (used_by_bc_target_count ≥ 1) enforced | ✓ |
| S2 (entity_slice_name non-null) enforced | ✓ |
| Idempotency precheck against 194 active BCs | ✓ (31 idempotent rows detected — would short-circuit at writer prewrite recheck) |
| No UNKNOWN rows hidden under another class | ✓ (288 UNKNOWN rows surfaced; all in compile report §12 triage queue) |

**Caveat 1 — Heuristic AR codes.** AR-1 (known_metric_pull) and AR-2 (source_chain_pull) require live mcf.* / contract.* substrate queries that A0 did not perform (heavier than the readonly allowlist warranted for the initial run). Operator review at A1–A5 should confirm per-row AR-1 / AR-2 evidence where applicable. AR-3 (entity_backbone_completeness) + AR-4 (standards_backed_foundation) are present on every executable row by construction.

**Caveat 2 — Pass 1 / Pass 2 / Pass 3 split.** This A0 run emits BC target rows (Pass 3 view). Pass 1 (characteristic admission) and Pass 2 (entity admission) are pre-requisites; the retry ledger captures the dependency via `prereq_ledger_uids`. A subsequent A0 run could emit explicit Pass 1 + Pass 2 stub rows. For A1–A5 review, the consolidated view in this report is sufficient.

## 16. Operator decision queue (held rows)

| Decision type | Count | Where it surfaces |
|---|---|---|
| RED held — composite-identity entity decision required | 67 | E4 / E6 / E9 / E12 composite waves |
| AMBER without pin_evidence_text — operator pin required | 639 | All AMBER rows by default; A1–A5 review pins per-row shape |
| DEFER — source-diagnostic / metadata / runtime | 1700 | Held indefinitely until pull condition surfaces |
| UNKNOWN — operator triage required | 288 | §12 triage queue above |

## 17. Non-actions in this A0 run

- No LLM / panel calls.
- No F3 / F4-v2 / createEntity / createCharacteristic / createBusinessConcept invocations.
- No certification confirmations.
- No DB writes.
- No `concept_registry.*` mutation.
- No `bcf.*` mutation.
- No `mcf.*` / `contract.*` reads (deferred — AR-1 / AR-2 evidence collected at A1–A5 review).
- No code edits affecting bc-core / bc-ai / bc-portal / bc-admin.
- No PR.
- No service restarts.

A0 is complete. Pending A1–A5 program approval. The sibling file `bcf-oagis-retry-ledger-2026-06-24.md` contains the per-row ledger seed.

## 18. Substrate verification appendix (operator-mandated row-level audit)

> **Discipline correction.** Operator flagged that the initial A0 substrate extraction relied on a compact `string_agg` summary for the 194-BC row set. Per corrected discipline: row-level extraction is authoritative; compact summaries are derived. SQL COUNT(*) is captured first; row-level extraction follows with pagination/chunking; extracted row count must equal SQL COUNT(*); halt on any mismatch. This appendix records the row-level audit that confirms the A0 output stands.

### 18.1 SQL COUNT(*) baseline (queried before any row extraction)

| Surface | Filter | SQL COUNT(*) |
|---|---|---|
| `concept_registry.entity` | (no filter) | 29 |
| `concept_registry.entity` | `lifecycle_state='active' AND archived_at IS NULL` | **26** |
| `concept_registry.characteristic` | (no filter) | 63 |
| `concept_registry.characteristic` | `lifecycle_state='active' AND archived_at IS NULL` | **62** |
| `concept_registry.business_concept` | (no filter) | 203 |
| `concept_registry.business_concept` | `lifecycle_state='active' AND archived_at IS NULL` | **194** |
| `concept_registry.business_concept` | `… AND kind='value'` | **194** |
| `concept_registry.business_concept` | `… AND kind='reference'` | 0 |

Difference between total and active = 3 superseded entities (Invoice Line Item, Payment, Shipment), 1 draft characteristic (`fiscal period`), 9 superseded BCs. All active BCs are `kind='value'`; no reference BCs in current substrate.

### 18.2 Row-level extraction (pagination as needed)

| Surface | Strategy | Rows returned | Match SQL COUNT(*)? |
|---|---|---|---|
| Active entities | Single query (26 rows ≤ MCP limit) | 26 | ✓ |
| Active characteristics | Single query (62 rows ≤ MCP limit) | 62 | ✓ |
| Active BCs | **Two chunks** (MCP cap 100): chunk 1 OFFSET 0 LIMIT 100 → 100 rows; chunk 2 OFFSET 100 LIMIT 100 → 94 rows | 100 + 94 = 194 | ✓ |

Every row carried `(id, entity_id_or_term_or_(entity_id+char_id), lifecycle_state, archived_at)` plus shape attributes (`representation_term`, `data_type`, `kind` for BCs).

### 18.3 Row-level cross-reference integrity

| Check | Result |
|---|---|
| All 194 BC rows resolve `entity_id` to one of the 26 active entities | ✓ |
| All 194 BC rows resolve `characteristic_id` to one of the 62 active characteristics | ✓ |
| Orphan references (BC pointing to unresolved entity or characteristic) | 0 |
| Distinct `(entity_name, characteristic_term)` tuples after row-level join | 194 |

The 1:1 ratio of BC rows to distinct `(entity, characteristic)` tuples confirms the substrate's `uq_business_concept_value_identity` partial unique index discipline (no duplicate active value BCs per (entity_id, characteristic_id) tuple — per DDL §6.3).

### 18.4 Comparison: embedded `ACTIVE_BC_TUPLES` (in compile script) vs authoritative row-level set

Verification script `barecount-devhub/scripts/_a0-verify-substrate.mjs` ran the set-equality check:

| Check | Result |
|---|---|
| Embedded tuple count (from `_a0-compile.mjs`) | 194 |
| Authoritative tuple count (from row-level extraction + resolution) | 194 |
| Tuples in embedded but NOT in authoritative | **0** |
| Tuples in authoritative but NOT in embedded | **0** |
| Set equality | **✓ PASS** |

### 18.5 Verdict

**Verification passed.** The A0 compile output (this report + `bcf-oagis-retry-ledger-2026-06-24.md`) stands as authoritative.

The compact `string_agg` summary used during initial extraction was a **derived view of correct underlying row data** — confirmed by the row-level audit that the embedded `(entity_name, characteristic_term)` set is byte-equal to what authoritative row-level extraction produces. No A0 output changes are required.

### 18.6 Discipline correction recorded for future A0 runs

| Rule | Status |
|---|---|
| Capture SQL COUNT(*) first, before any row extraction | **Applied for this audit; will be the standard for subsequent A0 runs.** |
| Row-level extraction is authoritative; compact summaries are derived only after row-level extraction | **Codified.** |
| Pagination / chunking when MCP row-count cap (100) is hit | **Codified.** Verified by 2-chunk extraction of the 194-BC set. |
| Verify extracted row count equals SQL COUNT(*) at each surface | **Codified.** |
| Halt on count mismatch — do NOT derive summaries before count parity confirmed | **Codified.** Verification script exits non-zero on any mismatch. |
| Read-only discipline preserved (no LLM, no panel, no substrate write) | **Confirmed.** |

### 18.7 Substrate state at audit end (re-confirmed)

A final SELECT count(*) at the close of this audit returned 26 / 62 / 194 — matching session-start state and confirming no substrate mutation during the A0 run or the audit.

| Surface | Session start | Audit end | Delta |
|---|---|---|---|
| Active entities | 26 | 26 | 0 |
| Active characteristics | 62 | 62 | 0 |
| Active value BCs | 194 | 194 | 0 |

A0 substrate-extraction discipline is now compliant with the operator's row-level-first rule. The compile report stands as authoritative.

## 19. Arithmetic correction appendix (mutually exclusive vs multi-label schema)

> **Operator-flagged blocker.** The initial §1 raw bucket table summed to **2,914** while the total scalar field count was reported as **2,868** — a **46-row discrepancy** that violated the apparent mutually-exclusive bucket schema. This appendix diagnoses the root cause, confirms which counting base is mutually exclusive, and corrects the schema. A0 substrate extraction is unaffected; the underlying compile logic is correct; only the report's labelling needed correction.

### 19.1 Diagnosis — role-split mapped scalar fields

The 46 over-count is entirely attributable to **6 role-split nouns** whose noun→entity mapping produces 2 target entities per scalar field that reaches the entity loop. For each such scalar field, the compile script emits **2 candidate rows** (one per target entity); both rows carry the same bucket assignment because role-split entity pairs are class-consistent.

Per-noun breakdown (verified by `scripts/_a0-arithmetic-check-v2.mjs`):

| Noun | Total scalar fields | Routed to DROP/DEFER | Reached buckets 1-4 | Candidate rows emitted | Role-split inflation |
|---|---|---|---|---|---|
| invoice | 36 | 17 | 19 | 38 | +19 |
| project-accounting | 14 | 7 | 7 | 14 | +7 |
| commercial-invoice | 20 | 14 | 6 | 12 | +6 |
| credit-transfer | 22 | 17 | 5 | 10 | +5 |
| debit-transfer | 22 | 17 | 5 | 10 | +5 |
| payment-status | 21 | 17 | 4 | 8 | +4 |
| **Total** | 135 | 89 | **46** | 92 | **+46** |

The remaining **127** role-split scalar fields routed to DROP/DEFER (single-counted, no inflation). Only the **46** that reached the entity loop produced the per-(field, entity) doubling.

### 19.2 Schema confirmation — mutually exclusive at which level?

The 8 buckets are **mutually exclusive at the candidate-row level** and at the **ledger-row level** — the two levels A0 uses operationally. They are NOT mutually exclusive at the **scalar-field level** for the 46 role-split-mapped fields, because each such field carries one primary classification plus one role-split secondary candidate row.

| Counting base | Mutually exclusive? | Total | Notes |
|---|---|---|---|
| **Scalar field** (1 per OAGIS field in extract) | Multi-label (1 primary + ≤1 role-split secondary) | **2,868** | The natural unit of the OAGIS extract |
| **Candidate row** (1 per (field, target-entity) pair) | **Yes — exactly one bucket per row** | **2,914** | The natural unit of the ledger seed and packet_hash computation |
| **Ledger row** (1 per (field, target-entity) for DROP/DEFER + 1 per deduped (entity, characteristic) for buckets 1-4) | **Yes — exactly one bucket per row** | **2,721** | The natural unit of the retry ledger persisted to Markdown / future Phase-2 DBCP |
| **Executable BC target** (1 per deduped (entity, characteristic) for buckets 1-4 only) | **Yes — exactly one bucket per row** | **733** | The natural unit of the executable backlog the writer authors |

The original §1 table reported View B (candidate-row count = 2,914) but mis-labelled the total as "scalar fields = 2,868". Both totals are correct; only the label was wrong. §1 now presents both views explicitly.

### 19.3 Reconciled count surfaces (all four bases)

| Surface | Value | Sums-to base |
|---|---|---|
| **§1.A View A** — per-scalar-field primary bucket (View A) | 112 + 297 + 108 + 363 + 48 + 1,561 + 91 + 288 | **2,868** ✓ (scalar fields) |
| **§1.B View B** — per-candidate-row bucket (View B) | 127 + 321 + 108 + 370 + 48 + 1,561 + 91 + 288 | **2,914** ✓ (candidate rows) |
| **§2 Deduplicated BC target counts** (per (entity, char) tuple in buckets 1-4) | 78 + 238 + 83 + 334 | **733** ✓ |
| **§3 Execution-class split** (per ledger row across all 8 buckets) | 27 + 639 + 67 + 1,700 + 288 | **2,721** ✓ (ledger rows = DROP+DEFER+HALT (1,988) + deduped BC targets (733)) |

All four bases reconcile internally and against the row-level substrate verification in §18.

### 19.4 Corrected BC dedup split (per §2)

The actual report §2 has shown the correct dedup split throughout:

| Bucket | Per-candidate-row | Deduplicated by (entity, characteristic) | Dedup rate |
|---|---|---|---|
| 1 EXISTING_ENTITY_EXISTING_CHARACTERISTIC_BC | 127 | **78** | 38.6% dedup |
| 2 EXISTING_ENTITY_NEW_CHARACTERISTIC | 321 | **238** | 25.9% dedup |
| 3 NEW_ENTITY_EXISTING_CHARACTERISTIC | 108 | **83** | 23.1% dedup |
| 4 NEW_ENTITY_NEW_CHARACTERISTIC | 370 | **334** | 9.7% dedup |
| **Total executable BC backlog** | 926 | **733** | 20.8% dedup overall |

A prior in-message summary mis-stated the per-bucket dedup split as 78 / 245 / 81 / 329 — that was a recall error in the summary, not a calculation error in the report. The report has consistently shown 78 / 238 / 83 / 334. **Authoritative figures: 78 / 238 / 83 / 334 = 733.**

### 19.5 Schema clarification for the retry ledger row

Each ledger row in `bcf-oagis-retry-ledger-2026-06-24.md` carries:

| Field | Cardinality per scalar field | Notes |
|---|---|---|
| `candidate_id` (LDG-NNNNN) | 1 per candidate row | Role-split fields produce 2 candidates with sequential IDs |
| `primary_bucket` | 1 per scalar field | The bucket the scalar field would have carried under View A |
| `candidate_row_bucket` | 1 per (field, target-entity) | The bucket the candidate row carries under View B |
| `role_split_secondary_target` | flag (boolean) | `true` on the second candidate row of a role-split scalar field; `false` on primary; absent for non-role-split fields |
| `target_entity` | 1 per candidate row | Distinct per role-split pair |
| `packet_hash` | 1 per candidate row | Each candidate row hashes independently |

For role-split scalar fields, `primary_bucket = candidate_row_bucket` because role-split entity pairs are class-consistent.

### 19.6 Execution-class counts (unchanged)

Execution class (GREEN / AMBER / RED / DEFER / UNKNOWN) counts per §3 are **per ledger row**:

| Class | Ledger rows | Comment |
|---|---|---|
| GREEN | 27 | Whitelist EE_EC_BC reuse — autonomous-safe |
| AMBER | 639 | Need pin_evidence_text per row |
| RED | 67 | Composite-identity entities held |
| DEFER | 1,700 | Metadata + runtime-derived + source-diagnostic (single-counted per scalar field) |
| UNKNOWN | 288 | SEMANTIC_AMBIGUITY_HALT (single-counted per scalar field) |
| **Total** | **2,721** | Matches §19.2 ledger-row base |

The execution-class totals do NOT change as a result of this correction. The §3 sum of 2,721 was already internally consistent; only the §1 label was misleading.

### 19.7 Cost estimate (unchanged)

The estimated panel-call count and spend range remain unchanged:

| Pass | Panel calls (est.) | Wall-time (h) |
|---|---|---|
| Pass 1 (~217 new chars) | 217 | ~3.6h |
| Pass 2 (~54 new entities) | 54 | ~0.9h |
| Pass 3 (~702 BC bindings — 733 minus 31 idempotent) | 702 | ~10.7h |
| **TOTAL** | **973 panel calls** | **~15.2 panel-execution hours** |
| Spend (low / mid / high) | | **$48.65 / $97.30 / $194.60** |

The 733 BC target count was correct in the original report; only the §1 raw-bucket label was misleading. Cost arithmetic is downstream of the deduplicated BC target count + Pass 1 / Pass 2 admission counts, which were all correct.

### 19.8 Decision and authoritative answer to the operator's question

**Are the 8 A0 classification buckets mutually exclusive?**

**Yes — at the candidate-row level (2,914 rows) and at the ledger-row level (2,721 rows).** Each candidate row and each ledger row carries exactly one bucket assignment.

**No — at the scalar-field level (2,868 fields)** — 46 role-split-mapped scalar fields each contribute primary + secondary candidate rows in the same bucket. The required schema at the scalar-field level is `primary_bucket` + `role_split_secondary_target` flag (codified in §19.5).

### 19.9 What this correction does NOT change

- Row-level substrate extraction (§18) unaffected.
- 26 active entities / 62 active characteristics / 194 active BCs unchanged.
- Deduplicated BC target count of **733** unchanged.
- 54 proposed new entities + 217 proposed new char bf_names unchanged.
- Execution-class split (27 GREEN / 639 AMBER / 67 RED / 1,700 DEFER / 288 UNKNOWN) unchanged.
- Per-(C/E/BC)-wave counts in §5 unchanged.
- Cost estimate (973 panel calls / $48–$195 spend / ~15.2h wall-time) unchanged.
- Recommended A1–A5 settings in §14 unchanged.

### 19.10 What this correction DOES change

- §1 now presents two explicit views: View A (per-scalar-field primary; sums to 2,868) and View B (per-candidate-row; sums to 2,914).
- §19 codifies the dual-view schema and the role-split inflation source.
- The retry ledger row schema (§19.5) is augmented with `primary_bucket` + `role_split_secondary_target` for future audit completeness.
- Compile script `scripts/_a0-compile.mjs` should be updated in a follow-on revision (under A4 program approval) to emit both views in its summary log.

### 19.11 Non-actions

- No panel calls.
- No LLM calls.
- No DB writes.
- No substrate mutations (verified by start-of-session and post-correction COUNT(*) queries: 26 / 62 / 194 unchanged).
- No `_a0-compile.mjs` code changes in this session (codification of the schema lives in §19; script update is deferred to post-A4).
- No A1–A5 entered.

A0 arithmetic is now reconciled and the report stands. A1–A5 remains operator-decidable; the executable BC target count of 733 + 31 idempotent + 54 entities + 217 char bf_names is unchanged. The blocker is resolved.

## 20. A1–A5 approval clarification — AR posture and AMBER pin scaling

> **Two operator-flagged approval blockers addressed.** (1) AR-1 known_metric_pull / AR-2 source_chain_pull evidence was not populated in A0 because `mcf.*` / `contract.*` were not queried — the report stated this but did not declare the program-scoped posture. (2) The 639 AMBER rows would each require an individual `pin_evidence_text` decision — unacceptable scale for autonomous broad buildout. This section provides the formal posture (§20.1), the exact counts (§20.2), the template-based pin model (§20.3), the A0.5 deterministic pin-template generator output (§20.4), and the verdict on whether A0.5 must run before A1–A5 (§20.5).

### 20.1 Recommended AR posture for the OAGIS broad foundation program

The program is a **foundation-buildout** in the sense of §1 Evidence-Governed Foundation Buildout. AR-3 and AR-4 are the load-bearing rationales for foundation programs; AR-1 / AR-2 are steady-state pulls more naturally suited to orphan cleanup (per orphan inventory §8) than to broad foundation expansion.

| Rationale | Posture for this program | When it applies |
|---|---|---|
| **AR-3 `entity_backbone_completeness`** | **Required & sufficient by construction.** Every executable row has it because every row binds (or admits a characteristic that binds to) an entity that has a documented entity_slice_name. | Default on every row. Enforced by safeguard S2 (entity_slice_name non-null). |
| **AR-4 `standards_backed_foundation`** | **Required & sufficient by construction.** Every executable row carries an OAGIS T2 citation in `oagis_provenance`. | Default on every row. OAGIS extract is the Tier-T2 evidence corpus per Vocabulary Evidence Framework §2. |
| **AR-1 `known_metric_pull`** | **Optional strengthening evidence.** Recommended where a concrete active MC (or seed MC with declared chain) cites the binding; not required to admit the row. The 27 GREEN whitelist BCs on AR / AP / GL almost certainly carry AR-1 (DSO / DPO / ARPI / trial balance metrics already cite these characteristics on these entities), but A0 did not query `mcf.*` to confirm. | Pinned at A1 per row IF operator wants to record metric provenance. Optional. |
| **AR-2 `source_chain_pull`** | **Optional strengthening evidence.** Recommended where a concrete authored SC → AC → OC → CC enrichment requires the binding for a tenant onboarding or seed configuration. A0 did not query `contract.*` to confirm. | Pinned at A1 per row IF operator wants to record source-chain provenance. Optional. |
| **AR-5 `operator_strategic_priority`** | **Operator-asserted strengthening evidence.** Used when operator declares a strategic initiative (e.g., "enable manufacturing vertical"). Not required for admission. | Pinned at A1 per row IF operator asserts the strategic priority. Optional. |

**Posture rationale.** The strict Evidence-Governed Foundation Buildout rule (§1.2) requires **at least one** of AR-1..AR-5 to admit a row. For this OAGIS broad buildout, every row carries AR-3 (entity backbone) + AR-4 (OAGIS standards) **by construction** because the program's whole purpose is to use OAGIS as standards-backed seed to complete entity backbones. The strict-pull alternative (require AR-1 or AR-2 per row) would block ~95% of the executable backlog given that the metric universe + source-chain universe for the proposed substrate has not been declared yet — the precise failure mode §1.1 was written to prevent.

### 20.2 Exact counts under recommended AR posture

| Surface | Count | Notes |
|---|---|---|
| **Rows executable under AR-3 + AR-4 only** | **666** | = 27 GREEN + 639 AMBER. Every row carries AR-3 + AR-4 by construction. |
| **Rows requiring AR-1 / AR-2 / AR-5 to be admissible** | **0** | None require strengthening — AR-1/AR-2/AR-5 are optional per-row enhancements. |
| **Rows where AR-1 strengthening is highly likely (operator should review at A1)** | ~78–105 | The 27 GREEN BC-AR/BC-AP whitelist rows + ~50–78 EE_EC_BC bindings on existing AR/AP/GL entities where active MCs (DSO, DPO, ARPI, trial-balance metrics) cite the characteristic. A subsequent A0 invocation with `mcf.*` read access would identify them deterministically. |
| **RED rows (composite-identity entities)** | 67 | Held regardless of AR posture; require separate operator decision packets per §1.4 + §5.1. |
| **DEFER rows** | 1,700 | Excluded by §3 filter; AR posture moot. |
| **UNKNOWN rows** | 288 | Operator triage required regardless of AR posture. |

### 20.3 Proposed AMBER pin-template model (replaces 639 individual decisions)

The 639 individual `pin_evidence_text` decisions are unscalable. The proposed template model groups rows by four orthogonal axes; each row inherits the appropriate template's pin unless it falls into a documented exception.

> **Authority discipline.** The Cat-1 clusters (6) and Cat-2 slices (13) are **review-organization tools** — they group rows so the operator can read the catalogue. The **pin authority** for Cat 1 is the **per-characteristic shape matrix** generated by A0.5 (`bcf-oagis-a0.5-template-catalogue-2026-06-24.md`, §1.C1–§1.C6), not the cluster name. Approving "C1 code cluster" does NOT blindly approve every code-like characteristic — the operator approves the explicit shape tuple per characteristic in the matrix. Same discipline for Cat 2: the per-entity admission decision in the slice's entity list is the pin authority, not the slice name.

| Cat | Template axis | What gets pinned (pin authority) | Coverage |
|---|---|---|---|
| **Cat 1** | Characteristic cluster (C1–C6) is review-organization; **per-characteristic shape matrix in the catalogue is the pin authority** | Per-row shape tuple `(representation_term, data_type, semantic_role)` from the matrix + F4-v2 v1 classification = Global. **A shape_conflict flag on a row requires per-row operator pinning, not blanket cluster approval.** | Pins shape for **217 new characteristic admissions** (Pass 1), each carrying its own row in the matrix |
| **Cat 2** | Entity slice (13 slices per §4B.2) is review-organization; **per-entity admission decision is the pin authority** | Per-entity admission: entity name + family + simple-vs-composite + identity-bearing graph for composites. Operator approves the entity list in each slice, NOT the slice as an abstract category. | Pins admission decisions for **54 new entity admissions** (Pass 2). The catalogue lists every entity under its slice for inspection. |
| **Cat 3** | OAGIS T2 evidence template — **citation format only** | Citation **format** pin: "OAGIS 10.12 noun.component.field cited verbatim per row." **Does NOT substitute for row-specific evidence**: every row still carries its exact `(noun, component, field_path)` provenance and `evidence_hash` per §6.3 schema. The template only standardises the citation format, not the citation content. | Citation format covers **all rows** (639 AMBER + 217 chars + 54 entities); per-row provenance + evidence_hash remain per-row. Operator may add per-domain specializations (e.g., procurement-specific noun citations) but is not required to. |
| **Cat 4** | Sibling-shape inheritance | **No separate template.** BC binding rows inherit shape from the characteristic (existing chars from substrate, new chars from Cat 1) | Subsumed |
| **RED** | Composite-identity entity decision packet | Identity-bearing reference graph + acyclicity confirmation per BCR §4 | 7 composite-RED entities → **7 separate decision packets** |

### 20.4 A0.5 deterministic pin-template generator — exact counts

A0.5 ran (script: `barecount-devhub/scripts/_a0.5-pin-templates.mjs`, scratch). Deterministic. No LLM. No panel. No substrate write.

**Category 1 — Characteristic cluster templates (Pass 1 shape pins):**

| Cluster | rep_term | Unique new-char bf_names |
|---|---|---|
| C1 | `code` | 40 |
| C2 | `date` | 46 |
| C3 | `identifier` | 58 |
| C4 | `amount` (incl. `rate`) | 31 |
| C5 | `quantity` / `count` / `measure` | 8 |
| C6 | `text` / `indicator` | 34 |
| **TOTAL** | | **217 new char bf_names → 6 templates** |

**Category 2 — Entity slice templates (Pass 2 admission pins):**

| Slice | New entities | Notes |
|---|---|---|
| master-data-root | 1 (Item) | Required first |
| master-data | 8 (Business Partner, Cost Centre, Location, Org Unit, Party, Price List, Price List Item, Project) | |
| procurement-transactional | 8 (PR + Line, Outline Agreement + Line, RFQ + Line, Quote + Line) | |
| logistics | 7 (Delivery, Pick List + Line, Shipment Request, Shipment Unit, Three-Way Match + Line) | |
| production-simple | 7 (Work Order, Routing, BOM, Production Confirmation, Production Schedule, Manufacturing Process, Work Centre) | |
| inventory-composite | 5 (Inventory Position + Movement + Movement Line + Count + Count Line) | Contains 1 composite-RED |
| quality-simple | 5 (Certificate, Corrective Action, Nonconformance + Line, Test Method) | |
| reference-warranty-budget | 3 (Budget Ledger Entry + Line, Warranty Claim) | |
| production-composite | 3 (Work Order Operation, Operation, BOM Line) | All composite-RED |
| asset-maintenance-simple | 3 (Asset, Equipment, Maintenance Order) | |
| quality-composite | 2 (Inspection Lot, Test Result) | All composite-RED |
| workforce | 1 (Employee) | |
| maintenance-composite | 1 (Maintenance Order Operation) | composite-RED |
| **TOTAL** | **54 new entities → 13 templates** | |

**Category 3 — OAGIS evidence template:**

A0.5 identified 100 distinct (noun, component) citation pairs across the AMBER row pool. Operator may approve **1 generic template** ("OAGIS 10.12 noun.component.field cited per row") that covers all rows, OR add per-domain specializations (~5–6 templates total). The recommended posture is the **single generic template** because every row in `oagis_provenance` carries the full path — specializations add ceremony without admission discrimination. **Template count: 1.**

**Category 4 — Sibling-shape inheritance: 0 separate templates.**

Shape is carried by the characteristic, not the entity. Existing characteristics carry their substrate shape; new characteristics admitted in Pass 1 inherit shape from the Cat-1 C-cluster template. BC binding rows therefore inherit shape automatically. A0.5 verified:

| Check | Result |
|---|---|
| Existing characteristics with multiple shapes in substrate | **0** (every active characteristic carries exactly one shape across all current BCs) |
| AMBER rows with rep_term disagreement vs dominant char cohort | **0** (every proposed BC binding's rep_term matches the dominant cohort for its characteristic) |
| Per-row shape-mismatch exceptions requiring operator pinning | **0 at A1; expected ~5–10 surfacing at execution time as panel parks** |

The Wave B `BSL × line_number` outlier (count/integer/amount vs the 5-sibling identifier/string/identity pattern) is the textbook Cat-4 exception — A0.5's deterministic check did not surface any equivalents in the current OAGIS-proposed backlog because the proposed shapes align with the dominant cohort per characteristic.

### 20.5 Estimated operator decision count under the template model

| Decision class | Count | Comment |
|---|---|---|
| Cat 1 char-cluster templates | **6** | One per C1–C6 cluster; covers 217 new chars |
| Cat 2 entity-slice templates | **13** | One per slice; covers 54 new entities |
| Cat 3 OAGIS evidence template | **1** (or up to ~6 per-domain specializations) | Single generic citation template covers all rows |
| Cat 4 sibling-shape templates | **0** | Subsumed by Cat 1 + existing substrate |
| RED composite-identity packets | **7** | One per composite entity (separate from templates; per §1.4 + §5.1) |
| **Total A1 operator decisions** | **27** | Approve at A1; recorded in ledger as program-level pins |
| Per-row exceptions surfaced at execution | **~5–10 expected** | Panel-time parks where actual shape mismatches the template's dominant cohort (Wave B §5.3 mechanism); operator triages post-execution, non-blocking |

**Reduction factor:** 27 template+packet decisions at A1 vs ~910 individual decisions (639 AMBER + 217 char admissions + 54 entity admissions) = **~34× reduction in operator A1-time decision load.**

### 20.6 Verdict — is A0.5 required before A1–A5?

**Yes.** A0.5 is required before A1–A5 program approval. Without it, operator cannot review and approve the template categories that scale the AMBER pinning — A1–A5 would degenerate to 639 individual decisions or to blanket approval without per-template scrutiny.

The A0.5 deterministic prototype has already produced the template counts above. Operator A1–A5 review reads:

1. **§20.1 AR posture statement** — confirm AR-3 + AR-4 sufficient; AR-1/AR-2/AR-5 optional.
2. **§20.3 + §20.4 template categories** — approve 6 Cat-1 + 13 Cat-2 + 1 Cat-3 = **20 templates** + 7 RED composite-identity packets = **27 A1 decisions total**.
3. **§20.5 exception expectation** — accept that ~5–10 per-row exceptions will surface at execution and be triaged then (Wave B §5.3 mechanism), not at A1.

The A0.5 output is the substrate that makes the A1–A5 review tractable. A future A0.5 revision integrated into the §6A Compile Layer would emit the templates as part of the standard compile artefact; for this run, the prototype script's output (above) is sufficient evidence for the operator's A1 review.

### 20.7 What §20 does NOT do

- Does not admit any characteristic, entity, or BC. A0.5 is deterministic and read-only.
- Does not call any LLM or panel.
- Does not write to any substrate.
- Does not mutate `concept_registry.*` or `bcf.*`.
- Does not approve A1–A5. The operator records A1–A5 only after reviewing this section + the templates above.
- Does not query `mcf.*` or `contract.*` to materialise AR-1 / AR-2 evidence (deferred — the recommended posture makes these optional strengthening, not required admission).

### 20.9 Catalogue file — the inspectable A1 surface

The A0.5 pin-template catalogue is held at:

**[`bcf-oagis-a0.5-template-catalogue-2026-06-24.md`](bcf-oagis-a0.5-template-catalogue-2026-06-24.md)** (sibling to this compile report under `bc-docs-v3/docs/implementation/`).

The catalogue is the **substrate of record** for the template inventory. The operator's A1 review reads the catalogue directly; this report's §20 is the summary, not a substitute. The catalogue contains:

| Catalogue section | Content | A1 use |
|---|---|---|
| §1.C1 — §1.C6 | Per-characteristic shape matrix for each cluster (one row per of the 217 proposed new characteristics, with bf_name, distinct shape tuple(s), shape_conflict flag, OAGIS sample, used_by_bc_target_count, target slices, execution class) | **Approve per-row shape pin**, not the cluster name |
| §2 | Intra-characteristic shape-conflict report (per-characteristic shapes that vary across OAGIS occurrences) | Triage any flagged characteristics before A1 |
| §3 | Same-rep_term-different-shape check (per-cluster multi-shape signal) | Confirm matrix is required (not cluster name) for clusters showing multi-shape |
| §4 | Existing-characteristic substrate-cohort comparison | Triage existing characteristics whose proposed shape from OAGIS differs from substrate dominant cohort |
| §5 | Cat-2 entity-slice list with each slice's entity inventory | **Approve per-entity admission** under each slice |
| §6 | Cat-3 citation format statement | **Approve citation format only**; per-row provenance + evidence_hash remain row-level |
| §7 | RED composite-identity packets list | Schedule separate operator decision packet per RED entity |
| §8 | A1 operator decision summary | Cross-check against §20.5 of this report |

**A1 reading order:** catalogue §1 (the matrix) → catalogue §2–§4 (conflict reports) → catalogue §5 (slices) → catalogue §6 (Cat-3) → catalogue §7 (RED) → catalogue §8 (decision summary) → this report's §20.5 + §20.6 (verdict).

### 20.10 Catalogue summary findings (from A0.5 catalogue generation)

| Finding | Count | Disposition |
|---|---|---|
| Proposed new characteristics with shape_conflict (multiple shapes within own OAGIS occurrences) | **0** | Clean — every proposed bf_name carries one shape across all its occurrences |
| Existing characteristics with multi-shape OAGIS proposals | **1** (`effective date`) | OAGIS proposes both `date/date/temporal` and `date/timestamp/temporal`; matches the known Wave A Customer Invoice × effective date timestamp-granularity case from scope audit §3.5. **Per-row pin at execution.** |
| Clusters with multiple distinct shape tuples (operator cannot collapse to single shape pin) | 6 of 6 | **Matrix is required for all clusters**, not cluster-name approval. Confirmed by A0.5. |
| 7 RED composite-identity entities held | 7 packets queued | Inventory Position · Work Order Operation · Operation · BOM Line · Inspection Lot · Test Result · Maintenance Order Operation |

### 20.8 Substrate state at append end (re-confirmed)

| Surface | Session start | §20 append end | Delta |
|---|---|---|---|
| Active entities | 26 | 26 | 0 |
| Active characteristics | 62 | 62 | 0 |
| Active value BCs | 194 | 194 | 0 |

A0 + A0.5 are complete. Both arithmetic blockers are resolved. **A1–A5 program approval is operator-decidable next, with the 27-decision template set above replacing 910 individual decisions.**

## 21. A1–A5 program authorization recorded (2026-06-24)

> **Status update.** Operator authorized recording of A1–A5 on 2026-06-24 with the instruction "Proceed to record A1–A5 program approval for the BCF × OAGIS broad foundation buildout." The recording is captured below; the canonical governance record is **[DEC-f94895 / D452](../adrs/ADR-f94895.md)**; the substrate-of-record is the retry ledger header at `bcf-oagis-retry-ledger-2026-06-24.md` with `program_authorization: active`.

### 21.1 What was recorded

| Approval | Value recorded |
|---|---|
| **A1 — Target-state catalogue + admission rule** | §1 Evidence-Governed Foundation Buildout adopted. AR-3 + AR-4 sufficient by construction; AR-1/AR-2/AR-5 optional strengthening. §4A catalogue + §4A.10 distribution + §4B layer-first re-aggregation accepted. A0.5 per-characteristic shape matrix (catalogue §1.C1–§1.C6) is the **Cat-1 pin authority**; Cat-2 entity-slice entity list (catalogue §5) is the **Cat-2 pin authority**; Cat-3 OAGIS T2 citation is **format only**. Safeguards S1 + S2 enforced. |
| **A2 — Row classification + admission rules** | §5.1 + §8.2 row execution rules. GREEN auto / AMBER auto-with-pin / RED held / DEFER held / UNKNOWN fatal stop. §1.2 admission rationale validation. §1.4 S1 + S2 safeguards. |
| **A3 — Halt rules** | §5.3 per-(C/E/BC)-wave halt rules + §8.4 22 fatal stops + §6A.0/§6A.1 compile-layer pre-conditions. |
| **A4 — Retry ledger + Compile + Cache + Coordinator** | §6 retry ledger (Phase-1 Markdown) + §6A A0 compile (only approved dry-run style) + §6B Batch + Cache + §6C Coordinator / Workers / Single Writer + §6.7 program-completion state. Concurrency defaults: A0 4–8, packet 4, panel 2→3 after 20 clean, writer 1, FIFO within dependency_group. |
| **A5 — Budget / time caps** | Total program spend cap: **$400 USD**. Total panel-call cap: **1,216**. Pass-level caps: P1 $80 / 270 calls · P2 $40 / 80 calls · P3 $280 / 880 calls. Per-row latency cap: **180 seconds**. Wall-time cap: **24 hours**. |

### 21.2 Open question resolutions

| OQ | Resolution |
|---|---|
| OQ-1 AMBER pin posture | Per-row pin_evidence_text from A0.5 catalogue matrix (do not wait for packet-builder enhancement) |
| OQ-13 RED row sequencing | Sequential one packet at a time |
| OQ-14 sequencing model | Layer-first per §1A |
| OQ-15 first-wave sequencing | Sequential C1 → E1 → BC-AR for first program execution |
| OQ-16 admission rationale review depth | Distribution-level at A1 + per-row matrix in catalogue |

### 21.3 RED composite-identity packets queued (held under A1–A5)

The 7 RED composite-identity entities remain **held**. A1–A5 recording does NOT authorize their execution; each requires a separate operator decision packet per §1.4 + §5.1 + BCR §4 acyclicity.

| Entity | Slice | Wave | Held status |
|---|---|---|---|
| Inventory Position | inventory-composite | E4 / BC-Inventory | Held — separate operator decision packet required |
| Work Order Operation | production-composite | E6 / BC-Production-composite | Held — separate operator decision packet required |
| Operation | production-composite | E6 / BC-Production-composite | Held — separate operator decision packet required |
| Bill of Materials Line | production-composite | E6 / BC-Production-composite | Held — separate operator decision packet required |
| Inspection Lot | quality-composite | E9 / BC-Quality-composite | Held — separate operator decision packet required |
| Test Result | quality-composite | E9 / BC-Quality-composite | Held — separate operator decision packet required |
| Maintenance Order Operation | maintenance-composite | E12 / BC-Asset | Held — separate operator decision packet required |

### 21.4 Known per-row pin at execution

| Surface | Item | Resolution path |
|---|---|---|
| Existing characteristic | `effective date` carries multi-shape OAGIS proposals (`date/date/temporal` vs `date/timestamp/temporal`); matches Wave A Customer Invoice case from scope audit §3.5 | Per-row pin at execution time; surfaces as panel-time park if not pre-pinned |

### 21.5 What this recording does NOT do

| Non-action | Status |
|---|---|
| Start panel execution | **Not started.** Execution-start gate is held. Separate operator instruction required to begin Pass 1 C1. |
| Touch `concept_registry.*` or `bcf.*` substrate | **No mutations.** Substrate confirmed unchanged at recording: 26 / 62 / 194. |
| Authorize RED row execution | **No.** RED rows remain held under their own decision packets. |
| Lock operator out of revising caps/decisions | **No.** Operator may amend by reply OR by superseding DEC-f94895 with a new ADR. |
| Authorize the microbatch panel design (§6B.6) | **No.** Future microbatch design remains explicitly NOT authorized. |
| Authorize Phase-2 ledger DBCP | **No.** Phase-1 Markdown ledger is the substrate-of-record. |

### 21.6 Provenance + audit trail

| Surface | Reference |
|---|---|
| Canonical governance record | **DEC-f94895 (D452)** — auto-generated ADR at `bc-docs-v3/docs/adrs/ADR-f94895.md` |
| Recording session | SES-934488 (operator: anant; 2026-06-24) |
| Substrate-of-record (operational state) | `bcf-oagis-retry-ledger-2026-06-24.md` header — `program_authorization: active` |
| A0 compile session | SES-3ef9dc (coordinator_run_id: CRR-a0c24a) |
| A0.5 catalogue | `bcf-oagis-a0.5-template-catalogue-2026-06-24.md` |
| Substrate snapshot hash | `sha256:8bcfa7a0bd220e304d2526574f2e0a18c8aa5bcc9a3e5bc8557d76273f46653d` |

### 21.7 Substrate state at recording end (re-confirmed)

| Surface | Before recording | After recording | Delta |
|---|---|---|---|
| Active entities | 26 | 26 | 0 |
| Active characteristics | 62 | 62 | 0 |
| Active value BCs | 194 | 194 | 0 |

**Recording is governance-metadata only; no substrate state changes.**

### 21.8 Next operator act

The program is now **`program_authorization: active`**. The execution-start gate remains **held**. The agent does not initiate Pass 1 C1 until a separate operator instruction lifts the gate. Possible next operator acts:

| Operator act | Effect |
|---|---|
| "begin Pass 1 C1" (or equivalent) | Lifts execution-start gate; agent begins F4-v2 v1 admission of 40 C1 characteristics with per-row pins from catalogue §1.C1 |
| "revise A5 cap to $X" (or equivalent) | Updates A5 cap value in retry ledger header; supersedes the cap recorded in DEC-f94895 with a new ADR |
| "draft RED packet for Inventory Position" (or equivalent) | Begins separate operator decision packet for one composite-identity entity; does not start execution of other passes |
| "hold" / "halt" / "withdraw" | Pauses or withdraws program authorization; flips header back to `program_authorization: withdrawn` |
| "query mcf.* for AR-1 evidence on EE_EC_BC rows" | Authorizes a subsequent A0 invocation with mcf.* read access to populate AR-1 strengthening evidence (optional; non-blocking) |

A1–A5 program authorization is recorded. **Execution awaits the operator.**

## 22. Pass 1 C1 execution attempt — halted at pre-execution service-health gate (2026-06-24)

> **Status update.** Operator issued the execution-start instruction "Begin Pass 1 C1 under DEC-f94895" on 2026-06-24 (SES-059790). Pre-execution gate per §8.4 fatal-stop list + §6C.2 service-health checks failed: bc-core and bc-ai are not running. The program halted before any panel call, substrate write, or row state change. A1–A5 `program_authorization=active` is preserved; the resume condition is documented below.

### 22.1 Gate checks performed (read-only)

| Check | Result | Source |
|---|---|---|
| bc-core port 3100 bound | **No** | `netstat -ano \| grep ":3100"` returned empty |
| bc-core `/api/health` reachable | **No** — `fetch failed` | Node `fetch('http://localhost:3100/api/health')` |
| bc-ai port 4300 bound | **No** | `netstat -ano \| grep ":4300"` returned empty |
| bc-ai `/health` reachable | **No** — `fetch failed` | Node `fetch('http://localhost:4300/health')` |
| F4-v2 v1 endpoint code exists | **Yes** — `POST /api/bcf/registry-authoring-runs` | `bc-core/src/registry/registry-authoring-panel/registry-authoring-run.controller.ts:27` |
| Hosting service running | **No** | Confirmed by all four prior checks |

### 22.2 §8.4 fatal-stop trigger fired

| Trigger | Detail |
|---|---|
| Service unavailable | bc-core + bc-ai both unreachable; F4-v2 v1 endpoint code present but offline. Per §8.4 this is a **program-level halt**, not a per-row park. |

### 22.3 What did NOT happen during Pass 1 C1 attempt

| Action | Status |
|---|---|
| Panel calls | **0** |
| LLM calls | **0** |
| Substrate writes | **0** (verified by post-halt SQL COUNT(*): 26 / 62 / 194 unchanged) |
| Certifications | **0** |
| Rows transitioned from `proposed` to `program_authorized` | **0** |
| F4-v2 v1 panel run records | **0** |
| Code edits to bc-core / bc-ai / bc-portal / bc-admin | **0** |

The halt fired at the pre-execution health-check step, **before** the row-execution loop in §8.5 began. No `coordinator_run_id` was minted for Pass 1 (the existing CRR-a0c24a is the A0 compile run, not an execution run).

### 22.4 What is preserved

| State | Value |
|---|---|
| A1–A5 program_authorization | **active** (recording from DEC-f94895 stands) |
| A0 retry ledger seed | **intact** (all 217 char rows + 54 entity rows + 733 BC target rows remain `proposed`) |
| A0.5 template catalogue | **intact** |
| Substrate snapshot hash | **unchanged** — substrate matches the hash recorded at recording time |
| Active substrate | **26 entities / 62 characteristics / 194 BCs** (verified before and after halt) |
| Budget caps | **0% consumed** ($0 of $400; 0 of 1,216 panel calls; 0h of 24h wall-time) |

### 22.5 Resume condition (operator action required)

To lift the halt and resume execution:

1. **Start Docker** (if not already): bc-core depends on Docker compose stack (`docker compose up -d` from `C:\MyProjects\bc-core`).
2. **Start bc-core**: `cd C:\MyProjects\bc-core && npm run start:dev` (binds port 3100; requires `barecount` AWS profile + Cognito + DB connections from `.env`).
3. **Start bc-ai**: `cd C:\MyProjects\bc-ai && uvicorn main:app --port 4300` (binds port 4300; bc-core uses it for Maker/Checker/Moderator panels).
4. **Verify health**: confirm both services respond on their health endpoints.
5. **Re-issue execution instruction**: "Begin Pass 1 C1 under DEC-f94895" (or equivalent).

A1–A5 state, A0/A0.5 ledger, and DEC-f94895 do NOT need re-recording. The resume is a service-availability gate-lift, not a re-authorization.

### 22.6 Alternative paths the operator may consider

| Alternative | Effect |
|---|---|
| Defer Pass 1 C1 until services are running | No state change; current halt persists; operator may re-issue execution instruction once services are up |
| Switch to a different pass / wave | Same gate failure applies — all passes require bc-core + bc-ai. The halt is at the program level, not the C1 level. |
| Withdraw program authorization | Operator issues "hold" / "halt" / "withdraw"; agent flips `program_authorization` to `withdrawn`; supersedes DEC-f94895 with a new ADR |
| Convert C1 to operator-direct admission (NOT recommended) | Would bypass F4-v2 v1 panel + safeguards; conflicts with A2 row execution rules; would require a separate operator decision packet + ADR change |

### 22.7 Compliance notes

| Concern | Resolution |
|---|---|
| Foundation Invariant VI (evidence is emitted, not inferred) | **Preserved.** Halt evidence is recorded in retry ledger header + this report + the SES-059790 change record. No inferred row state changes. |
| DEC-26b6e2 / DEC-fb0b12 (characteristic immutability discipline) | **Preserved.** No characteristic was admitted, amended, or superseded. |
| §6C.2 writer pre-write recheck | **Preserved.** Writer was never invoked because the halt fired before any panel call would have queued a write. |
| §6C.4 budget caps | **Preserved.** $0 of $400 spent; 0 of 1,216 panel calls made; 0h of 24h wall-time used. |
| §1 Evidence-Governed Foundation Buildout | **Preserved.** No row admitted; no AR rationale acted on. |

### 22.8 Substrate state at halt end (re-confirmed)

| Surface | Before halt | After halt | Delta |
|---|---|---|---|
| Active entities | 26 | 26 | 0 |
| Active characteristics | 62 | 62 | 0 |
| Active value BCs | 194 | 194 | 0 |

Substrate unchanged. The halt is clean.

A1–A5 program authorization remains **active**. The execution-start gate is **halted_pre_execution** pending operator service start + re-issue.





